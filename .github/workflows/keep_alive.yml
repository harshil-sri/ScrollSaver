name: Keep Alive Cron

on:
  schedule:
    - cron: '*/10 * * * *' # Every 10 minutes

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render URL
        run: |
          curl -s https://scrollsaver.onrender.com > /dev/null
          echo "Pinged Render URL to keep alive!"
